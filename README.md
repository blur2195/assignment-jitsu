To install dependencies, use:
# `npm i`

After done installing dependencies, use: 
# `npm start`
to start development server

To use mockData, use json-server:
# `npm install -g json-server`
# `json-server --watch shipments.json --port 3001`